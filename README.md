# timer_wheel

Hierarchical timer wheel for efficient timeout management

Single-file, zero-dependency Python CLI tool.

## Usage

```bash
python timer_wheel.py --help
```

## Test

```bash
python -c "import timer_wheel; timer_wheel.test()"
```
